<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kjuns.mapper.CampMapper">

	<select id="selectByTitle" parameterType="String" resultType="int">
		SELECT count(id) From kjuns_camp where title = #{title} and data_flag=1
	</select>

	<select id="getTotalCount" resultType="int" parameterType="String">
		SELECT count(*) FROM kjuns_camp where data_flag = 1
		<if test="title != null and title != ''">
			and title like CONCAT('%',#{title},'%')
		</if>
	</select>
	
	<select id="queryCampList" resultType="com.kjuns.model.Camp">
		SELECT id, title, content, thumbnail,user_id issuers, create_by createBy,create_date createDate 
		FROM kjuns_camp where data_flag = 1 
		<if test="title != null and title != ''">
			and title like CONCAT('%',#{title},'%')
		</if>
		order by create_date desc limit #{pageNo},#{pageSize}
	</select>
	
	<select id="selectById" parameterType="String" resultType="com.kjuns.model.Camp">
		SELECT id, title, content,thumbnail, user_id issuers, create_by createBy,create_date createDate  FROM kjuns_camp where id=#{0}
	</select>
	
	<select id="deleteCamp" parameterType="com.kjuns.model.Camp">
		update kjuns_camp set data_flag = 0, update_by = #{updateBy}, update_date = #{updateDate}  where id = #{id}
	</select>
	
	<insert id="addCamp" parameterType="com.kjuns.model.Camp" >
		insert into kjuns_camp (id,title,content,thumbnail,user_id,create_by,create_date,data_flag,update_by,update_date) 
		values(#{id},#{title},#{content},#{thumbnail},#{issuers},#{createBy},#{createDate},1,#{updateBy},#{updateDate})
	</insert>
	
	<update id="updateCamp" parameterType="com.kjuns.model.Camp">
		update kjuns_camp set title = #{title},thumbnail=#{thumbnail},user_id=#{issuers},content=#{content}, update_by = #{updateBy}, update_date = #{updateDate} where id = #{id}
	</update>
	
</mapper>