<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kjuns.mapper.ReportMapper">

	<select id="getTotalCount" resultType="int" parameterType="com.kjuns.model.Report">
		SELECT count(*) FROM kjuns_faq where data_flag = 1
		<if test="title != null and title != ''">
			and title like CONCAT('%',#{title},'%')
		</if>
		<if test="content != null and content != ''">
			and content like CONCAT('%',#{content},'%')
		</if>
	</select>
	
	<select id="queryReportList" resultType="com.kjuns.model.Report">
		SELECT id, title, content, user_id userId,create_by createBy,create_date createDate 
		FROM kjuns_report where data_flag = 1 
		<if test="title != null and title != ''">
			and title like CONCAT('%',#{title},'%')
		</if>
		<if test="content != null and content != ''">
			and content like CONCAT('%',#{content},'%')
		</if>
		order by create_date desc limit #{pageNo},#{pageSize}
	</select>
	
	
	<select id="deleteFaq" parameterType="com.kjuns.model.Report">
		update kjuns_report set data_flag = 0, update_by = #{updateBy}, update_date = #{updateDate}  where id = #{id}
	</select>
	
</mapper>